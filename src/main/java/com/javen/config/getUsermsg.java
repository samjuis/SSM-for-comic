/*
 * package com.javen.config;
 * 
 * import javax.annotation.Resource;
 * 
 * import org.springframework.stereotype.Component;
 * 
 * import com.javen.model.User; import com.javen.service.IUserService;
 * 
 * @Component public class getUsermsg {
 * 
 * @Resource private IUserService userService;
 * 
 * public int getUserid(String md5) { User user=new User();
 * user=userService.getUserinfo(md5); System.out.println(user); return
 * user.getId(); } public int getUsersu(String md5) { User user=new User();
 * user=userService.getUserinfo(md5); return user.getId(); } }
 */